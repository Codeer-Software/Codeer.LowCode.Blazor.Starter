@page "/login"
@layout Shared.EmptyLayout
@inject HttpClient HttpClient
@inject ServerConnection Server
@inject NavigationManager NavigationManager

<div class="login-center">
  <div>
    <h1>Login</h1>
    @if (_totp == null)
    {
      @if (_passwordEnabled)
      {
        <EditForm Model="_login" OnValidSubmit="LoginAsync">
          <div class="mb-2">
            User ID
            <InputText @bind-Value="_login.Id" class="form-control" autocomplete="username" />
          </div>
          <div class="mb-2">
            Password
            <InputText @bind-Value="_login.Password" type="password" class="form-control" autocomplete="current-password" />
          </div>
          <div class="mb-3">
            <InputCheckbox @bind-Value="_login.IsPersistent" id="IsPersistent" />
            <label for="IsPersistent" class="ms-1">Remember me?</label>
          </div>
          <button type="submit" class="btn btn-primary" disabled="@_busy">Login</button>
        </EditForm>
      }
      @foreach (var provider in _providers)
      {
        <div class="mt-2">
          <button type="button" class="btn btn-outline-primary w-100" disabled="@_busy" @onclick="() => ExternalLoginAsync(provider.Name)">Login with @provider.DisplayName</button>
        </div>
      }
    }
    else
    {
      @* 二要素認証 (TOTP)。サーバーの TotpLogin が有効なとき、パスワード成功後にコードを求める *@
      <EditForm Model="_totp" OnValidSubmit="VerifyTotpAsync">
        @if (_totp.Status == "setup")
        {
          <p class="mb-2">Scan the QR code with your authenticator app, then enter the 6-digit code it shows.</p>
          <img src="data:image/png;base64,@_totp.QrPngBase64" alt="QR code for authenticator app" class="border mb-2" style="width:200px;height:200px;" />
          <div class="small text-muted mb-2">Can't scan? Enter this key manually:<br /><code>@_totp.Secret</code></div>
        }
        else
        {
          <p class="mb-2">Enter the 6-digit code shown in your authenticator app.</p>
        }
        <div class="mb-3">
          Authentication code
          <InputText @bind-Value="_totp.Code" class="form-control" inputmode="numeric" maxlength="6" autocomplete="one-time-code" />
        </div>
        <button type="submit" class="btn btn-primary" disabled="@_busy">Verify</button>
        <button type="button" class="btn btn-secondary ms-1" disabled="@_busy" @onclick="BackToLogin">Back</button>
      </EditForm>
    }
    @if (_failed)
    {
      <div class="text-danger mt-3">@(_errorMessage ?? "Login failed.")</div>
    }
  </div>
</div>

@code {
  [SupplyParameterFromQuery]
  public string? ReturnUrl { get; set; }

  class LoginInfo
  {
    public string? Id { get; set; }
    public string? Password { get; set; }
    public bool IsPersistent { get; set; }
    public string? TotpCode { get; set; }
  }

  class LoginOptions
  {
    public bool Password { get; set; } = true;
    public List<LoginProvider> Providers { get; set; } = new();
  }

  class LoginProvider
  {
    public string Name { get; set; } = string.Empty;
    public string DisplayName { get; set; } = string.Empty;
  }

  //POST api/account/login の応答。二要素認証が無効なら status は "ok" だけ
  class LoginResult
  {
    public string Status { get; set; } = "ok";
    public string? Secret { get; set; }
    public string? QrPngBase64 { get; set; }
  }

  class TotpStep
  {
    public string Status { get; set; } = string.Empty;
    public string? Secret { get; set; }
    public string? QrPngBase64 { get; set; }
    public string? Code { get; set; }
  }

  readonly LoginInfo _login = new();
  bool _passwordEnabled = true;
  List<LoginProvider> _providers = new();
  TotpStep? _totp;
  bool _busy;
  bool _failed;
  string? _errorMessage;

  protected override async Task OnInitializedAsync()
  {
    //Web のログイン画面と同じ選択肢: パスワードのフォームと、サーバーに設定された外部 IdP ごとのボタン
    try
    {
      var options = await HttpClient.GetFromJsonAsync<LoginOptions>("api/account/login_options");
      if (options != null)
      {
        _passwordEnabled = options.Password;
        _providers = options.Providers;
      }
    }
    catch { }
  }

  async Task LoginAsync()
  {
    if (string.IsNullOrEmpty(_login.Id) || string.IsNullOrEmpty(_login.Password))
    {
      _failed = true;
      return;
    }

    _busy = true;
    _failed = false;
    _errorMessage = null;
    try
    {
      _login.TotpCode = null;
      var response = await HttpClient.PostAsJsonAsync("api/account/login", _login);
      if (!response.IsSuccessStatusCode)
      {
        _failed = true;
        return;
      }

      //二要素認証が有効なら status が setup / totp で返り、まだサインインしていない
      var result = await ReadResultAsync(response);
      if (result.Status == "setup" || result.Status == "totp")
      {
        _totp = new TotpStep { Status = result.Status, Secret = result.Secret, QrPngBase64 = result.QrPngBase64 };
        return;
      }
      if (result.Status != "ok")
      {
        _failed = true;
        return;
      }
      SignedIn();
    }
    catch
    {
      _failed = true;
    }
    finally
    {
      _busy = false;
    }
  }

  async Task VerifyTotpAsync()
  {
    if (_totp == null || string.IsNullOrWhiteSpace(_totp.Code))
    {
      _failed = true;
      _errorMessage = "Enter the authentication code.";
      return;
    }

    _busy = true;
    _failed = false;
    _errorMessage = null;
    try
    {
      _login.TotpCode = _totp.Code.Trim();
      var response = await HttpClient.PostAsJsonAsync("api/account/login", _login);
      if (!response.IsSuccessStatusCode)
      {
        _failed = true;
        return;
      }
      var result = await ReadResultAsync(response);
      if (result.Status != "ok")
      {
        _failed = true;
        _errorMessage = "Invalid authentication code.";
        return;
      }
      SignedIn();
    }
    catch
    {
      _failed = true;
    }
    finally
    {
      _busy = false;
    }
  }

  void BackToLogin()
  {
    _totp = null;
    _failed = false;
    _errorMessage = null;
    _login.Password = null;
    _login.TotpCode = null;
  }

  static async Task<LoginResult> ReadResultAsync(HttpResponseMessage response)
  {
    try
    {
      return await response.Content.ReadFromJsonAsync<LoginResult>() ?? new LoginResult();
    }
    catch
    {
      return new LoginResult();
    }
  }

  //外部 IdP: システムブラウザ (埋め込み WebView は Google が拒否する) でサーバーのチャレンジ URL を開く。
  //サーバーは Web と同じ OIDC フローを完了し、使い捨てチケット付きで ServerSettings.LoginCallbackUrl へ戻す。
  //チケットを ServerConnection が持つ認証 Cookie に交換する
  async Task ExternalLoginAsync(string provider)
  {
    _busy = true;
    _failed = false;
    _errorMessage = null;
    try
    {
      var authUrl = new Uri(Server.BaseAddress, $"api/account/login/{Uri.EscapeDataString(provider)}?mobile=true");
      var result = await WebAuthenticator.Default.AuthenticateAsync(authUrl, new Uri(ServerSettings.LoginCallbackUrl));

      if (result.Properties.TryGetValue("error", out var error))
      {
        _failed = true;
        _errorMessage = error switch
        {
          "user_not_registered" => "This account is not registered.",
          "guest_not_allowed" => "Guest accounts are not allowed.",
          "domain_not_allowed" => "This account's domain is not allowed.",
          _ => "Login failed."
        };
        return;
      }
      if (!result.Properties.TryGetValue("ticket", out var ticket))
      {
        _failed = true;
        return;
      }

      var response = await HttpClient.PostAsJsonAsync("api/account/login_ticket", new { Ticket = ticket });
      if (!response.IsSuccessStatusCode)
      {
        _failed = true;
        return;
      }
      SignedIn();
    }
    catch (TaskCanceledException)
    {
      //ユーザーがブラウザを閉じた
    }
    catch
    {
      _failed = true;
    }
    finally
    {
      _busy = false;
    }
  }

  void SignedIn()
  {
    //ログイン前に発行された antiforgery トークンは匿名ユーザーのものなので取り直す
    Server.ResetAntiforgeryToken();

    var returnUrl = string.IsNullOrEmpty(ReturnUrl) || ReturnUrl.StartsWith("login") ? "/" : "/" + ReturnUrl.TrimStart('/');
    NavigationManager.NavigateTo(returnUrl, true);
  }
}
